# 운영과 환경

이 문서는 현재 저장소 기준의 운영 triage 순서와 active validation 방식을 정리한다.

## 운영 요약
- 현재 active validation은 `py_compile`, import smoke, `create_app()` smoke다.
- startup KV backend는 Redis 우선이며, Redis connect/ping 실패 시 `FILE_KVSTORE_ROOT`(default: `local_kvstore`) 기반 File KV로 내려간다.
- local model asset default root는 `NTIS_LOCAL_MODELS_ROOT` 또는 repo 기본값 `D:\Project\python_project\ntis_domain_rag_chatbot\Models`다. e5 embedding defaults는 이 루트 아래 Hugging Face cache snapshot 디렉터리로 해석한다.
- planner prompt asset과 domain card는 `apps/prompts/*`를 모듈 기준 절대경로로 읽으며, 현재 working directory에 의존하지 않는다.
- `scripts/run_baseline_checks.ps1`는 retired validation entrypoint다.
- `tests/` tree는 현재 worktree에서 active gate로 보지 않는다.
- planner runtime defaults의 source-of-truth는 `apps/planner/planner_defaults.py`다.

## 먼저 볼 경로
- 로그: `REQ.START -> PLANNER.* -> RAG.* -> LLM.RESULT -> REQ.SUMMARY`
- planner defaults owner: `apps/planner/planner_defaults.py`
- app runtime owner: `apps/api/app_factory.py`, `apps/api/runtime.py`
- workflow owner: `apps/api/workflow_builder.py`, `apps/api/workflow_nodes.py`
- planner runtime owner: `apps/planner/planner_runtime.py`

## 현재 권장 검증
```powershell
python -m py_compile apps/api/app_factory.py apps/api/runtime.py apps/api/routes.py apps/chat/answer_generation.py apps/platform/settings.py apps/platform/metrics.py apps/retrieval/retrieval.py apps/retrieval/rag_base_orchestration.py apps/planner/prompt_asset_paths.py apps/planner/planner_context_cards.py apps/planner/planner_runtime.py tests/test_runtime_kv_fallback.py tests/test_settings_local_model_paths.py tests/test_planner_prompt_asset_paths.py tests/test_base_orchestration_dense_threshold_binding.py tests/test_answer_generation_reference_payloads.py tests/test_api_routes_reference_payloads.py tests/test_platform_metrics.py
python -c "import apps.planner.query_analysis, apps.planner.planner_runtime"
python -c "from apps.api.app_factory import create_app; create_app()"
python -m pytest tests/test_runtime_kv_fallback.py -q -p no:cacheprovider
python -m pytest tests/test_settings_local_model_paths.py -q -p no:cacheprovider
python -m pytest tests/test_planner_prompt_asset_paths.py -q -p no:cacheprovider
python -m pytest tests/test_base_orchestration_dense_threshold_binding.py -q -p no:cacheprovider
python -m pytest tests/test_answer_generation_reference_payloads.py tests/test_api_routes_reference_payloads.py -q -p no:cacheprovider
python -m pytest tests/test_platform_metrics.py -q -p no:cacheprovider
```

- `langgraph`가 있으면 `build_request_workflow().compile()`까지 확인한다.
- `tests/` tree가 있어도 전체 pytest collect-only나 broad rerun을 current gate로 간주하지 않는다. 이 문서에 적은 targeted subset만 운영 검증으로 본다.
- retired script를 실행하면 clear failure로 멈추는 것이 정상이다.

## 운영 triage
1. `REQ.START`에서 raw query와 request override 입력을 확인한다.
2. `PLANNER.SIGNALS`, `PLANNER.STAGE1`, `PLANNER.STAGE15`, `PLANNER.STAGE2`, `PLANNER.ASSEMBLE` 순서로 planner drift를 본다.
3. `RAG.RETRIEVAL_QUERY.RESOLUTION`, `RAG.RESULT`, `RAG.CONTEXT`에서 retrieval/evidence 흐름을 확인한다.
4. `LLM.RESULT`와 `REQ.SUMMARY`에서 answer selection, groundedness, state consistency를 확인한다.
5. startup에서 `Redis connection failed`가 찍히면 곧이어 `KV backend falling back to file store`와 `File KV backend ready`가 이어지는지 확인한다.

## 문서 갱신 규칙
- planner defaults owner가 바뀌면 이 문서와 `README.md`, `04_회귀기준과_점검.md`, `SESSION_HANDOFF.md`를 같이 갱신한다.
- streaming/debug payload shape가 바뀌면 이 문서와 `02_실행계약과_전략규칙.md`를 같이 갱신한다.
- retired validation entrypoint를 다시 되살리지 않는 한 baseline script 대신 manifest-driven pytest와 current guidance를 다시 맞춘다.

## Metrics defaults
- `apps/platform/metrics.py` default `GPU_UTIL_QUERY` uses `avg(DCGM_FI_DEV_GPU_UTIL{job="dcgm-ntis3", gpu=~"0|2"})`.
- `gpuUtilPercent` falls back to `0.0` when the GPU query returns no numeric samples, and it also degrades to `0.0` after logged GPU collection failures.
