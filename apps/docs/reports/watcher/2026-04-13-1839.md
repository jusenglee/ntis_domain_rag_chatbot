# 2026-04-13 Watcher Report

- run_at: `2026-04-13T18:39:45+09:00`
- branch_head: `고도화 / 3299300ecaa4834a77598659d584e1f2c3a6089c`
- bootstrap_note: last watcher memory expected `고도화 / 5a398fafc6e500c5947c25dc5a6d8b11b4806a35`; current HEAD differs, so this run treated the tree as a new inspection target.
- what_changed: wrote this watcher report only. Production code was not edited.

## inspected_files

- `AGENTS.md`
- `apps/docs/README.md`
- `apps/docs/00_ONBOARDING.md`
- `apps/docs/02_실행계약과_전략규칙.md`
- `apps/docs/03_운영과_환경.md`
- `apps/docs/04_회귀기준과_점검.md`
- `apps/docs/GOLDEN_TESTS.md`
- `apps/docs/SESSION_HANDOFF.md`
- `.agents/skills/ntis-rag-context-refine/SKILL.md`
- `.agents/skills/ntis-rag-context-refine/references/SKILL_revised_ko.md`
- `apps/conversation/context_router.py`
- `apps/conversation/request_facade.py`
- `apps/conversation/scope_resolver.py`
- `apps/conversation/followup_anchor.py`
- `apps/conversation/view_state.py`
- `apps/conversation/turn_trigger.py`
- `apps/conversation/turn_interpreter.py`
- `apps/prompts/context_router_v1.md`
- `apps/prompts/turn_trigger_v1.md`
- `apps/prompts/turn_interpreter_v1.md`
- `tests/conversation/test_context_router.py`
- `tests/conversation/test_followup_unification.py`
- `tests/conversation/test_turn_interpreter.py`
- `tests/conversation/test_view_state_recent_mentions.py`
- `tests/test_request_facade_context_router.py`
- `tests/test_planner_prompt_asset_paths.py`
- deleted surfaces: `pytest.ini`, `eval/sample_queries.jsonl`

## architecture_map

- `ConversationViewState` owns `visible_answer_manifest`, `active_scope`, `subject_index`, and `recent_mentions`.
- `request_facade.build_intent_payload()` now runs `turn_trigger -> turn_interpreter -> turn_policy -> scope_resolver`, then invokes `context_router` only when scope resolution is still clarification-shaped and `recent_mentions` exist.
- `context_router` is supposed to recover only an anchor seed plus `rewritten_query_hint`; planner still owns `mode`, `relation`, `target_cols`, and `join_key_mode`.
- the new recent-mention rescue path depends on three prompt assets under `apps/prompts/`: `context_router_v1.md`, `turn_trigger_v1.md`, `turn_interpreter_v1.md`.
- current prompt-asset regression coverage still checks only planner-stage prompt files, not the new follow-up prompt files.
- current docs still describe critical follow-up/golden scenarios, but the old machine-readable eval fixture lane is no longer present in the worktree.

## findings_by_priority

### P1

- import-light recent-mention regression gate is broken.
  `apps/conversation/context_router.py:15-21` imports `langchain_core` and LLM runtime helpers at module import time even though the public `route_context()` path is deterministic. `apps/docs/04_회귀기준과_점검.md:32-57` presents `tests/conversation/test_context_router.py` as a recommended targeted pytest, but the current run failed during collection with `ModuleNotFoundError: langchain_core` before any test body executed. This removes the cheapest runnable safety lane for recent-mention recovery.

- new follow-up prompt assets are runtime dependencies but are neither tracked nor covered by prompt-asset tests.
  Runtime now loads `apps/prompts/context_router_v1.md` from `apps/conversation/context_router.py:286`, `apps/prompts/turn_trigger_v1.md` from `apps/conversation/turn_trigger.py:158`, and `apps/prompts/turn_interpreter_v1.md` from `apps/conversation/turn_interpreter.py:806`. `git ls-files --error-unmatch` failed for all three files in this worktree, and `tests/test_planner_prompt_asset_paths.py:47-63` still asserts only `planner_stage*.md`. A partial commit or clean checkout can therefore silently degrade follow-up behavior back to heuristic fallback.

### P2

- executable golden/eval coverage was removed without a replacement.
  The current diff deletes `eval/sample_queries.jsonl`, and the current tree has no `eval/` directory. At the same time `apps/docs/GOLDEN_TESTS.md` still documents contract-critical scenarios like ordinal follow-up, source reference, broad history, and year-axis recent-mention recovery. That leaves the scenarios documented but no longer machine-checkable in this worktree.

- validation/bootstrap state is still environment-opaque.
  `apps/docs/03_운영과_환경.md:21-27` and `:50-100` describe `create_app()` smoke and targeted pytest as the active validation posture, but this run could not execute them in the current environment: `create_app()` failed with `ModuleNotFoundError: fastapi`, and the targeted router/interpreter subset failed collection with `ModuleNotFoundError: langchain_core`. I could not find a tracked dependency manifest (`requirements*.txt`, `pyproject.toml`, similar), so it is unclear whether this is intentional env management outside the repo or repo bootstrap drift.

## user_quality_risks

- `마지막 과제 상세`, `2025년꺼`, and similar recent-mention rescues can ship with only heuristic fallback if the new prompt assets are omitted from commit or deploy.
- the lowest-cost regression lane for the new router layer is no longer collectible in a minimal environment, so anchor-selection regressions can slip until a heavier LLM-capable environment catches them.
- no direct evidence of new SEARCH people-name hard-must drift, fallback-chat mode rewrite, or `pjt_id`/`pjt_no` confusion was found in the touched code. The main risk in this run is validation and coverage drift around the new follow-up rescue layer.

## recommended_next_patch

- keep `route_context()` import-light by moving `langchain_core`, parser, and LLM runtime imports inside `run_context_router()`, then git-add the three new follow-up prompt assets and extend prompt-asset regression coverage to include them.

## recommended_new_tests

- extend `tests/test_planner_prompt_asset_paths.py` or add a sibling asset-path test that asserts `context_router_v1.md`, `turn_trigger_v1.md`, and `turn_interpreter_v1.md` resolve from `apps/prompts/` under changed cwd.
- add a deterministic import/behavior regression for `tests/conversation/test_context_router.py` that does not require `langchain_core`.
- add one request-facade or `/query/stream` transcript regression for recent-mention rescue (`마지막 과제 상세`, `2025년꺼`) proving planner fields remain planner-owned and no internal error leaks.
- restore a tiny machine-readable golden/eval lane for `ordinal`, `source_ref`, `quoted_title`, and `broad_history`.

## docs_to_sync

- `apps/docs/03_운영과_환경.md`
- `apps/docs/04_회귀기준과_점검.md`
- `apps/docs/GOLDEN_TESTS.md`
- optionally `apps/docs/PRODUCT_BASELINE.md` if the eval fixture lane is intentionally retired rather than restored

## validation

- passed: `python -m py_compile apps\conversation\context_router.py apps\conversation\request_facade.py apps\conversation\scope_resolver.py apps\conversation\followup_anchor.py apps\conversation\view_state.py apps\conversation\turn_interpreter.py apps\conversation\turn_trigger.py`
- failed: `python -c "from apps.api.app_factory import create_app; create_app()"` → `ModuleNotFoundError: fastapi`
- failed: `python -m pytest tests\conversation\test_context_router.py tests\test_request_facade_context_router.py tests\conversation\test_view_state_recent_mentions.py tests\conversation\test_followup_unification.py tests\conversation\test_turn_interpreter.py -q -p no:cacheprovider` → collection blocked by `ModuleNotFoundError: langchain_core`

## remains_risky

- because the dependency bootstrap is not described in-repo, this run cannot cleanly separate “expected local env missing packages” from “repo drift removed the bootstrap contract”.
- because the new prompt assets are untracked, the current worktree can look healthy locally while a future commit or clean checkout drops the rescue behavior.
