# 2026-04-15 Watcher Report

- run_at: `2026-04-15T18:41:11+09:00`
- branch_head: `고도화 / 3299300ecaa4834a77598659d584e1f2c3a6089c`
- bootstrap_note: current branch/head still matches the prior watcher baseline recorded on `2026-04-14`. This pass inspected the same dirty refactor worktree rather than a clean checkout.
- what_changed: wrote this watcher report only. Production code was not edited.

## inspected_files

- `AGENTS.md`
- `apps/docs/README.md`
- `apps/docs/01_시스템_계층과_2층_계약.md`
- `apps/docs/02_의도진실(L1)_정의와_식별자.md`
- `apps/docs/03_행동안전(L2)_정책과_보정.md`
- `apps/docs/04_도구화_규격과_상태_격리.md`
- `apps/docs/05_단계적_리팩토링_로드맵.md`
- `apps/docs/reference/PRODUCT_BASELINE.md`
- `apps/docs/reference/CODEX_CONTEXT.md`
- `apps/docs/reference/GOLDEN_TESTS.md`
- `apps/docs/reference/SESSION_HANDOFF.md`
- `apps/docs/reports/watcher/2026-04-14-1840.md`
- `.agents/skills/ntis-rag-context-refine/SKILL.md`
- `.agents/skills/ntis-rag-context-refine/references/SKILL_revised_ko.md`
- `apps/conversation/request_facade.py`
- `apps/conversation/context_router.py`
- `apps/conversation/turn_trigger.py`
- `apps/conversation/turn_interpreter.py`
- `apps/retrieval/runtime_routing.py`
- `apps/retrieval/execution_manager.py`
- `apps/retrieval/filters.py`
- `apps/api/routes.py`
- `apps/chat/llm_streaming.py`
- `apps/platform/openai_compat_llm.py`
- `apps/prompts/context_router_v1.md`
- `apps/prompts/turn_trigger_v1.md`
- `apps/prompts/turn_interpreter_v1.md`
- `tests/conversation/test_context_router.py`
- `tests/test_request_facade_context_router.py`
- `tests/test_planner_prompt_asset_paths.py`
- `tests/test_execution_manager_search_recovery.py`
- `tests/test_lookup_join_hybrid_runtime_failure.py`
- `tests/test_api_routes_reference_payloads.py`

## architecture_map

- `apps/docs/README.md` now presents the new `01-05` two-layer document set as the human source of truth and sends QA/operators to `reference/` for golden and handoff material.
- `apps/conversation/request_facade.py:1596-1727` runs `turn_trigger -> turn_interpreter -> turn_policy -> scope_resolver`, then optionally invokes `run_context_router()` to rescue clarification-shaped follow-ups from `recent_mentions`.
- `apps/conversation/context_router.py:259-326` keeps planner-owned fields banned and only allows recent-mention candidate selection plus an optional `rewritten_query_hint`.
- `apps/retrieval/runtime_routing.py:123-189` routes `project LOOKUP/SEARCH/JOIN` and `people/org/perf SEARCH` into `ExecutionManager`; `apps/retrieval/execution_manager.py:376-489` still keeps `instance` vs `group` JOIN recovery separated by `pjt_id` vs `pjt_no`.
- `apps/api/routes.py:801-849` still degrades `StrategyViolation` errors into a user-visible final answer instead of falling back to generic chat or a silent internal-error transcript.
- `apps/platform/openai_compat_llm.py:357-377` emits reasoning-only stream chunks as `content=""` with `additional_kwargs["reasoning_text"]`; `apps/chat/llm_streaming.py` is responsible for turning those chunks into TTFT and reasoning metrics.

## findings_by_priority

### P0

- none confirmed in this pass.

### P1

- the current source-of-truth docs are still outside Git tracking while the old tracked docs are staged for deletion.
  `apps/docs/README.md:12-16` and `:27-37` point contributors at the new `01-05` contract docs plus `reference/`, but `git status --short --untracked-files=all -- apps/docs ...` shows the tracked legacy docs deleted and the new `apps/docs/01-05`, `apps/docs/reference/*`, and watcher reports still untracked. A clean checkout would therefore lose the architecture baseline, handoff history, and golden catalog that the README now declares authoritative.

- the executable regression and prompt surface is still effectively untracked.
  `git ls-files -- tests` returned nothing, `pytest.ini` is currently a tracked deletion, and `git status` shows the active follow-up prompt assets (`apps/prompts/context_router_v1.md`, `apps/prompts/turn_trigger_v1.md`, `apps/prompts/turn_interpreter_v1.md`) plus their regressions (`tests/conversation/test_context_router.py`, `tests/test_request_facade_context_router.py`, `tests/test_planner_prompt_asset_paths.py`) as untracked. The tests themselves show real coverage for the new path (`tests/conversation/test_context_router.py:5-33`, `tests/test_request_facade_context_router.py:65-142`), but none of it survives a clean clone or partial commit.

- reasoning-first stream observability is still wrong in the current tree.
  `apps/platform/openai_compat_llm.py:357-377` intentionally emits reasoning-only chunks with empty visible content, but `apps/chat/llm_streaming.py:33-45` only parses `content`, and `apps/chat/llm_streaming.py:206-234` drops empty-content chunks before `first_chunk_at` or `reasoning_chars` are recorded. A fresh local repro with a fake reasoning-first stream returned `reasoning_chars=0` and `ttft_any_ms == ttft_content_ms == 119.5`, even though a reasoning event arrived about 50 ms earlier. This keeps reasoning-first stalls invisible to stream triage and answer-stage logging.

### P2

- the SEARCH people/org same-object gate still looks correct in code, but its focused regression is still missing from tracked ownership.
  `apps/retrieval/filters.py:690-780` still forces `participant_researcher_name + people_affiliation_org_name` into the same `prtcp_mp` nested gate and hardens the org gate with `_wrap_filter_in_must_gate()` when a person condition exists. This pass found no active semantic violation, but there is still no tracked `tests/test_people_filter_nested_gate.py`-style guard to catch future drift.

- bootstrap and validation remain unreproducible from the repo alone.
  `apps/docs/reference/PRODUCT_BASELINE.md:8-13` still says the active gate is `py_compile + create_app() + targeted pytest subset`, but `python -c "from apps.api.app_factory import create_app; create_app()"` still fails with `ModuleNotFoundError: fastapi`, and `rg --files -g 'requirements*.txt' -g 'pyproject.toml' -g 'Pipfile' -g 'poetry.lock'` found no tracked dependency manifest. I cannot tell from the repo alone whether this is an intentional external env contract or missing bootstrap metadata.

## user_quality_risks

- a clean checkout can lose the current architecture contract, golden catalog, follow-up prompts, and regression suite even when the dirty local tree looks healthy.
- recent-mention rescue behavior such as `마지막 과제 상세` or `2025년꺼` can disappear without any tracked prompt-path or request-facade regression failing.
- reasoning-first provider stalls can be under-reported as normal content TTFT, which weakens streaming incident triage and can hide degraded answer latency.
- people/org SEARCH semantics and degraded `/query/stream` behavior still depend more on untracked local tests than on tracked repo ownership.

## recommended_next_patch

- make the refactor state reviewable before touching more runtime logic: track the already-written `apps/docs/01-05` set, `apps/docs/reference/*`, the new watcher reports, `apps/prompts/context_router_v1.md`, `apps/prompts/turn_trigger_v1.md`, `apps/prompts/turn_interpreter_v1.md`, and the existing `tests/` regressions in one coherent non-production patch, while either restoring `pytest.ini` or replacing it with the intended validation entrypoint. Until that happens, the repo cannot reliably preserve its own current contract surface.

## recommended_new_tests

- add a direct `tests/test_llm_streaming.py` regression for reasoning-first streams that asserts `ttft_any_ms < ttft_content_ms`, `reasoning_chars > 0`, and awaited `aclose()` behavior.
- restore or add a focused `tests/test_people_filter_nested_gate.py` that locks same-object `participant_researcher_name + people_affiliation_org_name` semantics inside one `prtcp_mp` nested gate.
- extend prompt-path coverage so `tests/test_planner_prompt_asset_paths.py` (or a sibling test) also asserts cwd-independent loading for `context_router_v1.md`, `turn_trigger_v1.md`, and `turn_interpreter_v1.md`.
- add one route-level `/query/stream` regression proving a `StrategyViolation` still emits degraded `answer.final + reference.set + done` rather than a raw `error` event.

## docs_to_sync

- `apps/docs/README.md`
- `apps/docs/01_시스템_계층과_2층_계약.md`
- `apps/docs/03_행동안전(L2)_정책과_보정.md`
- `apps/docs/05_단계적_리팩토링_로드맵.md`
- `apps/docs/reference/PRODUCT_BASELINE.md`
- `apps/docs/reference/GOLDEN_TESTS.md`
- `apps/docs/reference/SESSION_HANDOFF.md`

## validation

- passed: `git rev-parse --abbrev-ref HEAD` -> `고도화`
- passed: `git rev-parse HEAD` -> `3299300ecaa4834a77598659d584e1f2c3a6089c`
- passed: `python -m py_compile apps/chat/llm_streaming.py apps/platform/openai_compat_llm.py apps/conversation/context_router.py apps/conversation/request_facade.py apps/retrieval/filters.py apps/api/routes.py`
- passed: `python -m pytest tests/test_planner_prompt_asset_paths.py tests/test_lookup_join_hybrid_runtime_failure.py tests/test_execution_manager_search_recovery.py -q -p no:cacheprovider` -> `9 passed`
- failed: `python -c "from apps.api.app_factory import create_app; create_app()"` -> `ModuleNotFoundError: fastapi`
- reproduced: a fake reasoning-first stream through `run_llm_streaming()` returned `reasoning_chars=0` and `ttft_any_ms == ttft_content_ms`, confirming the metrics bug above on the current tree.

## remains_risky

- because the new docs, prompts, and tests are still untracked, this report is describing a local worktree reality that the repository itself does not yet preserve.
- because the repo still lacks a tracked dependency/bootstrap manifest, the automation cannot distinguish expected local-package gaps from genuine startup regressions.
- because the current route-level and people-gate protections live mostly in untracked tests, I can say the code still looks bounded today, but not that a future clean checkout will keep those bounds.
