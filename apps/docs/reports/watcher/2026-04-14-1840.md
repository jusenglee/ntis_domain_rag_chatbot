# 2026-04-14 Watcher Report

- run_at: `2026-04-14T18:40:45+09:00`
- branch_head: `고도화 / 3299300ecaa4834a77598659d584e1f2c3a6089c`
- bootstrap_note: current branch/head matches the prior watcher report baseline (`2026-04-13-1839.md`). The worktree is still heavily dirty, so this pass treated the present uncommitted tree as the inspection target.
- what_changed: wrote this watcher report only. Production code was not edited.

## inspected_files

- `AGENTS.md`
- `apps/docs/README.md`
- `apps/docs/03_행동안전(L2)_정책과_보정.md`
- `apps/docs/04_도구화_규격과_상태_격리.md`
- `apps/docs/05_단계적_리팩토링_로드맵.md`
- `apps/docs/reference/CODEX_CONTEXT.md`
- `apps/docs/reference/GOLDEN_TESTS.md`
- `apps/docs/reference/SESSION_HANDOFF.md`
- `apps/docs/reports/watcher/2026-04-13-1839.md`
- `.agents/skills/ntis-rag-context-refine/SKILL.md`
- `.agents/skills/ntis-rag-context-refine/references/SKILL_revised_ko.md`
- `apps/conversation/context_router.py`
- `apps/conversation/request_facade.py`
- `apps/chat/llm_streaming.py`
- `apps/platform/openai_compat_llm.py`
- `apps/retrieval/runtime_routing.py`
- `apps/retrieval/execution_manager.py`
- `apps/retrieval/filters.py`
- `apps/prompts/context_router_v1.md`
- `apps/prompts/turn_trigger_v1.md`
- `apps/prompts/turn_interpreter_v1.md`
- `tests/conversation/test_context_router.py`
- `tests/test_request_facade_context_router.py`
- `tests/test_runtime_routing.py`
- `tests/test_execution_manager_people_org_search_observation.py`
- `tests/test_execution_manager_search_recovery.py`
- `tests/test_answer_generation_execution_trace_summary.py`
- `tests/test_planner_prompt_asset_paths.py`

## architecture_map

- `request_facade.build_intent_payload()` now runs `turn_trigger -> turn_interpreter -> turn_policy -> scope_resolver`, then optionally calls `run_context_router()` when the scope result is still clarification-shaped and `recent_mentions` exist.
- `context_router` remains bounded: `route_context()` is deterministic first, and `run_context_router()` may invoke a constrained LLM fallback that is only allowed to return a candidate index plus optional `rewritten_query_hint`. Planner-owned fields (`mode`, `relation`, `target_cols`, `join_key_mode`) are still explicitly banned in the prompt contract.
- `ExecutionManager` and `runtime_routing` now own `project LOOKUP / SEARCH / JOIN` plus `people/org SEARCH` observation-only routing. The tracked routing and execution-manager tests confirm those boundaries are green in this environment.
- `run_llm_streaming()` sits between provider adapters and answer generation. The provider adapter emits reasoning-only chunks through `additional_kwargs` while keeping user-visible `content=""`, and answer generation consumes TTFT and chunk metrics from `run_llm_streaming()`.
- The active docs were reorganized into the new `01-05` 2-layer set, but the concrete follow-up/golden/watchlist surfaces still live only under the untracked `apps/docs/reference/` tree and prior watcher reports.

## findings_by_priority

### P1

- runtime-critical recent-mention prompt assets are still untracked, and the only path-regression test is also untracked.
  `apps/conversation/context_router.py:259-326` now loads `context_router_v1.md`, and the companion follow-up stack also depends on `turn_trigger_v1.md` and `turn_interpreter_v1.md`. `git ls-files` returned nothing for all three prompt files and for `tests/test_planner_prompt_asset_paths.py`. The test itself (`tests/test_planner_prompt_asset_paths.py:47-63`) still asserts only `planner_stage*.md` prompt resolution. A clean checkout or partial commit can therefore drop the recent-mention rescue layer without any tracked regression failing.

- streaming TTFT and reasoning observability are currently wrong for reasoning-first streams.
  `apps/platform/openai_compat_llm.py:357-377` intentionally emits reasoning-only chunks with `content=""` and `additional_kwargs["reasoning_text"]`. But `apps/chat/llm_streaming.py:206-220` drops empty-content chunks before it records `first_chunk_at`, `first_content_at`, or `reasoning_chars`. In a local repro using a fake stream that yielded one delayed reasoning-only chunk followed by content, `run_llm_streaming()` reported `reasoning_chars=0` and `ttft_any_ms == ttft_content_ms == 118.5`. That means reasoning-first stalls are invisible to the current metrics and `answer_generation` stream guard logic.

### P2

- the SEARCH people-name hard-must contract still exists in code, but its dedicated regression is no longer tracked.
  `apps/retrieval/filters.py:1356-1512` still enforces the nested `prtcp_mp` same-object gate shape for people/org conditions. The currently tracked tests only cover the newer observation-routing boundary (`tests/test_execution_manager_people_org_search_observation.py:18-143`) and SEARCH recovery filter dropping (`tests/test_execution_manager_search_recovery.py`), while `git ls-files tests/test_people_filter_nested_gate.py` returned nothing. This pass found no active violation, but that safety contract is no longer protected by a tracked focused test.

- bootstrap validation is still unreproducible from the repo alone.
  Targeted pytest is green in the current shell, but `python -c "from apps.api.app_factory import create_app; create_app()"` still fails with `ModuleNotFoundError: fastapi`, and `git ls-files` found no tracked dependency manifest (`requirements*.txt`, `pyproject.toml`, `Pipfile`, etc.). `apps/docs/05_단계적_리팩토링_로드맵.md` claims a dependency-complete `107 passed` closeout, but the repo does not currently describe how automation should reach that environment.

## user_quality_risks

- `마지막 과제 상세`, `2025년꺼`, and similar recent-mention rescues can silently disappear in a clean checkout or deploy if the untracked prompt assets are omitted.
- reasoning-first provider behavior can be misclassified as healthy content-start timing, weakening stream triage for delayed or stalled answers.
- future people/org filter refactors can reintroduce hidden hard-must or wrong nested semantics without an early focused regression catching it.
- automation and new contributors still cannot tell whether startup-smoke failure is an expected local dependency gap or repo bootstrap drift.

## recommended_next_patch

- fix `apps/chat/llm_streaming.py` first so reasoning-only chunks contribute to `ttft_any_ms` and `reasoning_chars` before the empty-content early-continue. Add a direct regression for reasoning-first streams in the same patch. This is the smallest code change that closes a live observability bug affecting user-visible streaming reliability.

## recommended_new_tests

- add a direct `run_llm_streaming()` regression that feeds a reasoning-only chunk before content and asserts `ttft_any_ms < ttft_content_ms`, `reasoning_chars > 0`, and awaited `aclose()` behavior.
- extend `tests/test_planner_prompt_asset_paths.py` to cover `context_router_v1.md`, `turn_trigger_v1.md`, and `turn_interpreter_v1.md`, then track that file with the prompt assets.
- restore a focused `tests/test_people_filter_nested_gate.py`-style regression for `participant_researcher_name + people_affiliation_org_name` same-object semantics.
- add one `/query/stream` or request-facade transcript regression for recent-mention rescue proving planner-owned fields stay banned and no internal error leaks through the stream path.

## docs_to_sync

- `apps/docs/README.md`
- `apps/docs/03_행동안전(L2)_정책과_보정.md`
- `apps/docs/05_단계적_리팩토링_로드맵.md`
- `apps/docs/reference/GOLDEN_TESTS.md`
- `apps/docs/reference/CODEX_CONTEXT.md`

## validation

- passed: `git rev-parse --abbrev-ref HEAD` → `고도화`
- passed: `git rev-parse HEAD` → `3299300ecaa4834a77598659d584e1f2c3a6089c`
- passed: `python -m py_compile apps\conversation\context_router.py apps\conversation\request_facade.py apps\conversation\turn_interpreter.py apps\conversation\turn_trigger.py apps\retrieval\runtime_routing.py apps\retrieval\execution_manager.py apps\chat\llm_streaming.py`
- passed: `python -m pytest tests\conversation\test_context_router.py tests\test_request_facade_context_router.py tests\conversation\test_turn_interpreter.py tests\test_runtime_routing.py tests\test_execution_manager_people_org_search_observation.py tests\test_answer_generation_execution_trace_summary.py -q -p no:cacheprovider` → `29 passed`
- passed: `python -m pytest tests\test_planner_prompt_asset_paths.py -q -p no:cacheprovider` → `3 passed`
- passed: `python -m pytest tests\test_execution_manager_search_recovery.py -q -p no:cacheprovider` → `4 passed`
- failed: `python -c "from apps.api.app_factory import create_app; create_app()"` → `ModuleNotFoundError: fastapi`
- reproduced: a fake reasoning-first stream through `run_llm_streaming()` returned metrics with `reasoning_chars=0` and `ttft_any_ms == ttft_content_ms`, confirming the streaming observability bug above.

## remains_risky

- because the prompt assets and some of the new docs/tests are still untracked, this worktree can look locally correct while a clean checkout loses critical follow-up behavior and its intended docs.
- because the repo still lacks a tracked dependency/bootstrap contract, watcher automation cannot separate expected local package gaps from real startup-regression drift.
- because the dedicated nested people-gate regression is absent, this pass can only say the contract still looks correct in code today, not that it is guarded against future drift.
