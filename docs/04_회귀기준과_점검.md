# ?? ??? ??

? ??? follow-up, planner merge, retrieval short-circuit ?? ?? ?? ??? ????.

## 1. ?? follow-up ???

- ?? ? `??? ??? ???? ???`
  - `Reference Context` 1? ??? `pjt_id`? seed? ????? ??.
  - `hm_nm=["???"]` ?? people filter? ???? ? ??.
- ?? ? `??? ?? ?? ???`
  - ??? ??? deterministic?? ????? ??.
- ?? previous item ? `? ??? ???? ???`
  - deictic follow-up?? resolve ??? ??.
- ?? previous item ? `? ??? ???? ???`
  - ?? ???? ?? clarification ?? ??.
- ?? ? `??? ??? ??? ???`
  - project seed? ??? ? relation follow-up? ????? ??.

## 2. Sanitization ??

- planner filters? `???`, `???`, `???`, `? ??`? researcher/org/title ???? ???? strip ??.
- strip ? `PLANNER.FILTER.ORDINAL_STRIPPED` ??? ??? ??.
- seed? ?? ??? free-text filter?? seed? ???? ??.

## 3. Clarification ??

- ?? ?? ?? `??? ??`? ???? clarification??.
- ?? ?? 3? ? `??? ??`? ???? clarification??.
- explicit follow-up clarification? retrieval ?? short-circuit ? ? ??? ??.

## 4. ?? ??

- `python -m py_compile apps/api/main.py apps/api/routes.py apps/api/runtime.py apps/api/services/request_facade.py apps/api/services/planner_runtime.py apps/api/services/planner_service.py apps/api/services/rag_retriever.py apps/api/services/retrieval_workflow.py apps/core/planner_staged.py apps/core/followup_resolution.py apps/core/filters.py`
- `python -m pytest tests/test_planner_stagewise.py`

## 5. ?? ???

- `pjt_id`? `pjt_no`? ??? ???
- `strategy_meta.seed_source`? ?? truth? ???? ???
- unresolved explicit follow-up? ???? retrieval? ???? ???
- summary ??? follow-up resolution metadata? ???

## 6. View state 회귀 기준

- 목록 응답 후 `view_state.latest_display_snapshot`이 저장되는지 확인한다.
- raw retrieval count와 visible count가 분리돼 기록되는지 확인한다.
- `3번째 과제` 후속 질의가 visible snapshot 기준 3번 item으로 resolve되는지 확인한다.
- `그 과제` 후속 질의가 `latest_focus_entity`를 재사용하는지 확인한다.

## 7. Detail fallback 회귀 기준

- detail 응답에서 `DetailCoverage.detail_level`이 `not_found/profile_only/rich_detail` 중 하나로 계산되는지 확인한다.
- `profile_only` 데이터에 대해 전체 거절 대신 기본 프로필 + 부족한 상세 안내가 응답되는지 확인한다.
- 동일 entity에 대한 후속 detail 질의에서 `DETAIL.CACHE.HIT`가 발생하는지 확인한다.
- 서버 detail fallback이 적용되면 `ANSWER.FALLBACK.APPLIED` 로그가 남는지 확인한다.

## 8. 권장 검증 명령 추가

- `python -m py_compile apps/api/services/view_state.py apps/api/services/followup_anchor.py apps/api/services/detail_contract.py apps/api/services/request_facade.py apps/api/services/planner_runtime.py apps/api/services/retrieval_workflow.py`
- `python -m pytest tests -q --ignore-glob='tests/pytest-cache-files-*'`
